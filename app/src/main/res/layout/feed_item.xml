<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android" xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools">
    <data>
        <import type="com.raul.androidapps.softwareteststarling.R" />
        <variable name="feed" type="com.raul.androidapps.softwareteststarling.model.Feed"/>
        <variable name="resources" type="com.raul.androidapps.softwareteststarling.resources.ResourcesManager"/>
        <variable name="amount" type="String"/>
        <variable name="saving" type="String"/>


    </data>
    <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content">
        <TextView
                android:id="@+id/reference"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                tools:text="External Payment"
                android:text="@{feed.reference}"
                android:textSize="14sp"
                android:textStyle="bold"
                android:layout_marginStart="16dp"
                android:layout_marginTop="8dp"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintStart_toStartOf="parent"
        />
        <TextView
                android:id="@+id/name"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                tools:text="Mickey Mouse"
                android:text="@{feed.counterPartyName}"
                android:textSize="14sp"
                app:layout_constraintTop_toBottomOf="@id/reference"
                app:layout_constraintStart_toStartOf="@id/reference"
        />
        <TextView
                android:id="@+id/savigns_text"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                tools:text="@string/potential_saving"
                android:text="@{feed.sentToGoal == true? resources.getString(R.string.current_saving) : resources.getString(R.string.potential_saving)}"
                android:textSize="14sp"
                app:layout_constraintTop_toBottomOf="@id/name"
                app:layout_constraintStart_toStartOf="@id/name"
        />
        <TextView
                android:id="@+id/savigns_value"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                tools:text="0.56"
                android:text="@{saving}"
                setTextColor="@{feed.sentToGoal == true? resources.getColor(R.color.colorGrey) : resources.getColor(R.color.colorIn)}"
                android:textSize="14sp"
                app:layout_constraintTop_toTopOf="@id/savigns_text"
                app:layout_constraintBottom_toBottomOf="@id/savigns_text"
                app:layout_constraintStart_toEndOf="@id/savigns_text"
                android:layout_marginStart="8dp"
        />
        <TextView
                android:id="@+id/value"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                tools:text="GBP"
                tools:textColor="@color/colorOut"
                android:text="@{amount}"
                setTextColor="@{feed.direction.equals(`IN`)? resources.getColor(R.color.colorIn) : resources.getColor(R.color.colorOut)}"
                android:textSize="14sp"
                android:textStyle="bold"
                android:layout_marginEnd="16dp"
                android:layout_marginTop="8dp"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
        />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>